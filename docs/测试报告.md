# 系统完整测试报告

## 测试范围

- 后端自动化测试：62 项全部通过
- 10 大核心板块真实 MT5 集成验收：通过
- 前端全功能 UI 冒烟测试：37 项全部通过，浏览器零报错
- 前端生产构建：通过

## 10 大板块验收结果

| 板块 | 验收内容 | 结果 |
|---|---|---|
| 一、K线与历史数据 | MT5 K 线获取、图表渲染 | 通过 |
| 二、实时Tick与盘口 | `/api/mt5/tick`、WebSocket、前端实时盘口 | 通过 |
| 三、品种信息与点差 | 全品种列表、品种属性、pip_value、异常点差告警 | 通过 |
| 四、AI策略与信号生成 | 框选生成因子、沙盒校验、ML 模型训练与预测、信号扫描 | 通过 |
| 五、回测与历史复盘 | 自定义回测（品种/日期/周期/入金/杠杆/延迟/滑点）、模拟盘、自动复盘 | 通过 |
| 六、技术指标与特征工程 | 统一指标库、`/api/indicators`、前端 SMA/EMA/RSI/MACD 叠加 | 通过 |
| 七、动态风控与仓位 | MT5 品种参数手数计算、挂单超时撤单 | 通过 |
| 八、OEMS订单与持仓 | 因子 CRUD、执行器状态、订单日志、市价/挂单/止损限价、平仓撤单 | 通过 |
| 九、告警与消息推送 | 通道配置、Telegram/企业微信/钉钉/飞书 payload 测试、告警测试 | 通过 |
| 十、账户与心跳监控 | MT5 账户资金、系统心跳、断线重连告警 | 通过 |

## AI 训练与因子链路

- 多模态/模板因子生成：通过
- AST 沙盒静态检查与独立进程执行：通过
- 逻辑回归动量模型训练：通过（真实 MT5 K 线）
- 参数自动优化与自定义达标目标（胜率/收益/盈亏比/回撤，全部达标才算成功）：通过
- 参数优化样本外验证（walk-forward）与最低交易数约束：通过
- 权益风控熔断（单日亏损/最大回撤自动暂停匹配）：通过
- 订单幂等键防重复下单：通过
- 启动实盘匹配完整参数组（品种/周期/置信度/方向/仓位/止损止盈/延迟/风控）：通过
- 止损/止盈分开设置（ATR/固定点数/支撑压力位自由组合，缺失关键位回退 ATR）：通过
- 三阶段移动止损/止盈（盈利达标跟随、接近止盈锁定、突破后移动止盈并同步跟随、实盘修改 SL/TP 与订单日志、模拟盘同步）：通过
- 订单日志/持仓/最近执行同步展示止损与止盈（SL/TP），移动止损日志同时记录 SL/TP 更新：通过
- 实盘匹配状态入库与后端重启自动恢复（开关/品种/周期/执行参数/风控参数）：通过
- AI 管理模块（多配置 CRUD、8 大板块指派、连接测试、API Key 脱敏、顶部显眼状态、qwen3.8-max 因子生成接入与本地模板回退）：通过
- 形态案例库：形态指纹保存、入场/出场时机学习、止盈止损建议、实盘相似形态匹配：通过
- 形态匹配适配门槛（相似度门槛、最小样本数、时间衰减、市场环境加成）：通过
- 完整形态特征向量、多尺度指纹、结构/关键位相似度、DTW 与加权综合分、形态类型标签、亏损案例自动降权：通过
- 滚动样本外验证（多段）与参数复杂度惩罚：通过
- 模型预测与信号输出：通过
- 自定义历史回测（含日期区间、延迟、杠杆、滑点）：通过
- 因子库增删改查：通过

## 前端操作便捷性

- 7 个功能标签页全部可切换
- 品种搜索、框选/平移/标注模式切换、撤销/清除标注、指标开关
- 实时行情 WebSocket、指标图例、订单日志详情展开
- 浏览器控制台零错误

## 完整 UI 自动化测试（模拟账户）

使用真实 Chrome 自动点击全部核心 UI 功能：
- 基础 UI 冒烟：55 项全部通过
- 完整 UI 全功能：18 项全部通过

- 启动/暂停实盘匹配
- UI 发送 MT5 订单（BTCUSD 0.01）并确认持仓
- UI 一键平仓
- 导出中文复盘报告
- 框选 + 入场/出场精确点位 + AI 因子生成
- 运行历史回测、因子入库、清空因子库、二次入库
- 模拟盘启动/暂停
- 立即扫描并执行
- 因子编辑弹窗与保存
- 自定义回测（品种/日期/入金/杠杆/延迟）
- 参数自动优化（含自定义达标目标与未达标提示）
- 压力位/支撑位标记、撤销、清除
- 标记出场点后选择“出场原因”（到压力位 / 破支撑 / 止盈 / 止损 / 超时 / 手动）
- 自动建议入场/出场（基于最近支撑/压力位生成回踩支撑入场、到压力位出场）
- 启动实盘匹配止损方式选择“支撑/压力位”并显示缓冲参数
- 启动实盘匹配止损/止盈分开设置、移动止盈与止盈锁定参数
- 启动实盘匹配移动止损开关与激活盈利/ATR 追踪参数
- 启动实盘匹配移动止损备注说明
- AI 管理页面与顶部 AI 状态显示、AI 配置新增/删除
- 启动实盘匹配状态持久化：启动后重启后端自动恢复运行中状态
- 删除因子

测试中发现并修复：运行历史回测/自定义回测成功后自动跳转到回测结果标签，避免用户看不到结果。

当前状态：因子库保留用户 GOLD 因子（M15×2、M30×1），MT5 持仓 0 笔。

自动化脚本：
- `ui_layer/scripts/ui-smoke.mjs`：基础 UI 冒烟 55 项
- `ui_layer/scripts/ui-full-smoke.mjs`：完整 UI 全功能 18 项（含真实模拟账户下单、参数优化与门槛配置，测试数据自动清理，不影响用户因子库）
