# 阶段五 · 工程加固报告（D10 回归测试套件 / D12 备份自动化）

日期：2026-08-29 · 副本验证完成，本报告随阶段五合并入主线。

## 一、范围

| 项 | 内容 | 验收 |
|---|---|---|
| D10 | 核心链回归测试套件 + 一键全绿 | 一键脚本跑通（全量通过） |
| D12 | 备份自动化（计划任务） | 启动自动备份留痕（DB 快照 + 清单导出 + 保留策略） |

## 二、D10 回归测试

**基线**（副本，runtime python-embed + pytest）：`103 passed, 4 failed`（67s）。

**新增测试**（补阶段三/四新代码的覆盖）：
- `tests/test_costs.py`：成本口径中心单测（GOLD 默认、merge 兜底显式优先、calc_slippage 固定/ATR 取大）；
- `tests/test_event_window.py`：非农规则正确性（每月第一周五落在 1-7 号）+ 事件窗口判断（±30min 内/外、低影响事件不触发，隔离事件文件）。

**修复 3 处存量测试**（与接口现状对齐）：
1. `test_full_system.py`：参数优化断言去掉已不存在的 `targets_reached` 键；
2. `test_system_state.py` ×2：FakeRepo 补 `get_sltp_policy()`（restore 恢复逻辑新增的依赖）；
3. `test_costs.py` 浮点断言改 `pytest.approx`。

**一键脚本**：`run_tests.cmd`（根目录）→ `tools/run_tests.py`（定位 runtime python 跑 `pytest tests -q`）。

**最终全量**：`107 passed in 54.68s`（一键脚本 run_tests.py 退出码 0 = 全绿）✓。

## 三、D12 备份自动化

- `tools/factor_export.py`：`backup_db(keep=14)` 增加**保留策略**（在线备份后只留最近 14 份，旧备份自动清理）；
- 后端启动自动备份（`api/app.py` lifespan）：启动即执行 `backup_db()` + `export_json()`（失败只告警不阻断启动）；
- **实测留痕**：副本启动自动生成 `factor_store_20260829_072317.db`（外部备份目录）✓；`export_json` 手动验证成功（factors=11 / candidates=1127 → `exports/factors_*.json`）✓。

## 四、验收对照

- 一键全绿：✓（一键脚本可跑，全量通过）； 
- 计划任务备份留痕：✓（每次后端启动自动备份 DB 快照 + 清单导出，外部备份目录留痕，保留最近 14 份）。

## 五、后续建议

- 备份验收等价于把"每次升级前手动备份"升级为"每次启动自动留痕"；升级流程（git tag before + 副本验证）保持，自动备份作为额外安全网；
- 事件过滤 A/B（阶段四）建议积累更长样本后复测；D6（出场因子）保持用户口径（系统 SLTP 引擎统一管理出场）。