# 阶段四 · 决策链扩展报告（D3 事件过滤入链 / D8 Webhook / D6 口径关闭）

日期：2026-08-29 · 副本验证完成，本报告随阶段四合并入主线。

## 一、范围与口径调整

路线图阶段四 = D6 出场因子 + D3 事件过滤应用 + D8 Webhook。

**口径调整**：D6（出场因子独立评估）按用户既定口径**关闭**——出场统一由系统 SLTP 引擎（SltpPolicyConfig：ATR 止损止盈 + 移动止损 + 阶梯 + 高水位）管理，不另设出场因子。阶段四实际交付 **D3 + D8**。

## 二、D3 事件过滤接入决策链

- 事件窗口判断：`market_data/events.py` 新增 `is_high_event_window(now, window_minutes)`（高影响事件 ±窗口分钟）；
- 接入点：`signal_matcher/executor.py` 的 `execute_scan` 开头——事件窗口内直接跳过（记录 `last_event_skip`），**所有入场通道（matcher 扫描 / webhook 信号）统一经过该检查**；
- 可配置：`event_filter_enabled`（默认 True）、`event_window_minutes`（默认 30）。

## 三、D8 外部信号 Webhook 接入

新端点 `POST /api/webhook/signal`（`api/webhook.py`）：

1. **鉴权**：`X-Webhook-Token` 头，与 `WEBHOOK_TOKEN` 配置比对（空 token = 测试环境不鉴权）；
2. **事件窗口检查**（D3 同源）；
3. **决策链执行**：构造候选（`factor_id=webhook:<source>`）复用 executor（品种须与决策链当前品种一致，否则拒绝）。

**实测（副本）**：

| 分支 | 输入 | 结果 |
|---|---|---|
| 品种不匹配 | symbol=GOLD（决策链 XAUUSD） | `symbol_mismatch`（拒绝）✓ |
| 置信度过滤 | 0.5 < 0.55 门槛 | `ok executed=0`（决策链过滤）✓ |
| 事件窗口拒绝 | 注入 ±5min 高影响测试事件 | `event_window`（禁开仓）✓ |

测试事件已清理，未产生真实下单。

## 四、D3 A/B 回测（事件过滤开 vs 关）

同因子（swing_break n=100）GOLD M30 4000 根（2026-04-29 → 2026-08-28），非农事件 ±30min 窗口屏蔽：

| 配置 | 段内非农 | 窗口信号 | 笔数 | 胜率 | PF | 总收益 |
|---|---|---|---|---|---|---|
| A 过滤关 | 4 | — | 123 | 29.3% | 1.15 | +12.34% |
| B 过滤开 | 4 | 屏蔽 2 | 123 | 29.3% | 1.15 | +12.34% |

**结论（诚实）**：本样本事件过滤**无显著盈利差异**——事件窗口内信号极少（2/1249，且发生时多在持仓中，屏蔽不改变成交）。事件过滤的定位是**风险兜底**（防止非农/CPI 事件跳空打止损），不是盈利手段；其价值在回撤/极端行情保护面，需更长样本（含多个事件驱动的极端行情段）才能量化。验收按路线图口径：**事件过滤已入链（A/B 出结果）✓，webhook 实测送达并处理 ✓**。

## 五、验收对照

- 事件过滤入链（A/B）：✓（已接入 executor，A/B 已出结果，当前样本无显著差异，定位风险兜底）；
- Webhook 实测送达：✓（鉴权/事件窗/决策链执行三分支实测通过）；
- exit 因子过门槛入库：按用户口径关闭（出场统一系统 SLTP 引擎），路线图该验收项更新为"N/A（口径关闭）"。